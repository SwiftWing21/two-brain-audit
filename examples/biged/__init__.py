"""BigEd CC reference implementation — 12 audit dimensions.

This is the original use case that the two-brain-audit system was extracted
from. BigEd CC is a 125-skill AI fleet with a web dashboard, and these
12 dimensions cover every aspect of the system.

Usage:
    from examples.biged import create_biged_engine
    engine = create_biged_engine()
    results = engine.run_tier("daily")
"""
